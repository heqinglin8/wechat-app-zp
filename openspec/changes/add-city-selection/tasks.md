## 1. 共享城市模型

- [x] 1.1 新增共享城市工具模块，定义默认城市 `广东省 / 广州市 / 天河区`、缓存 key、开放城市列表和城市对象字段结构。
- [x] 1.2 实现从本地缓存读取当前城市，缓存缺失或非法时回退默认广州天河。
- [x] 1.3 实现从微信 `picker mode="region"` 的 `value` 和 `code` 归一化城市对象。
- [x] 1.4 实现开放城市校验，仅允许广州、佛山、韶关、深圳、东莞、珠海切换。
- [x] 1.5 实现城市展示名生成和最多 4 个中文的省略展示支持。
- [x] 1.6 实现城市切换成功后同步 `app.globalData.currentCity` 和本地缓存。

## 2. 城市选择入口

- [x] 2.1 新增可复用城市选择组件或页面片段，使用微信 `picker mode="region"` 打开省市区选择器。
- [x] 2.2 在首页左上角挂载城市选择入口，展示当前城市名并支持切换开放城市。
- [x] 2.3 实现首页选择未开放城市后的 toast：“暂时还没有开放当前城市业务”，并确认不切换、不缓存。
- [x] 2.4 将首页左上角城市选择入口点击宽度增大约 50%，同时保持城市名最多 4 个中文省略展示。

## 3. 列表与搜索过滤

- [x] 3.1 封装 `JobInfo` 查询追加当前 `cityCode` 条件的辅助逻辑。
- [x] 3.2 封装 `JobSeeker` 查询追加当前 `cityCode` 条件的辅助逻辑。
- [x] 3.3 首页首次加载、分类切换和加载更多岗位时按当前 `cityCode` 过滤，并保留现有排序、分类和分页规则。
- [x] 3.4 今日招聘首次加载、分类切换和加载更多岗位时按当前 `cityCode` 过滤，并保留现有排序、分类和分页规则。
- [x] 3.5 今日求职首次加载、分类切换和加载更多求职信息时按当前 `cityCode` 过滤，并保留现有排序、分类和分页规则。
- [x] 3.6 搜索结果查询同时使用 `cityCode == 当前城市 cityCode` 和 (`title` 模糊匹配搜索关键词 OR `jobDescription` 模糊匹配搜索关键词 OR `companyName` 模糊匹配搜索关键词)。
- [x] 3.7 搜索结果对标题命中、岗位描述命中和公司名命中的合并结果按 `objectId` 去重，并保持稳定排序。
- [x] 3.8 在首页、今日招聘、今日求职、搜索结果页检测城市变化，城市码变化时清空旧列表并重新加载。
- [x] 3.9 首页、今日招聘、今日求职查询结果为 0 时展示“暂无信息”和“重新刷新”按钮，点击后按当前城市和当前分类重新查询。
- [x] 3.10 搜索结果新增 `JobSeeker` 查询，按 `cityCode == 当前城市 cityCode` 且 (`title` 模糊匹配搜索关键词 OR `recoJobIntent` 模糊匹配搜索关键词) 过滤，并与 `JobInfo` 混合展示。
- [x] 3.11 搜索结果为 `JobInfo` 和 `JobSeeker` 使用不同样式 item，并按结果类型跳转岗位详情或求职详情。

## 4. 发布岗位城市写入

- [x] 4.1 在 `pages/publishjob/publishjob` 表单中展示当前省、市、区/县。
- [x] 4.2 在岗位发布页展示小字提醒“只能发布本城市的信息”，且不提供表单内城市修改入口。
- [x] 4.3 提交 `JobInfo` 时写入 `provinceName`、`cityName`、`districtName`、`provinceCode`、`cityCode`、`districtCode`、`cityDisplayName`。
- [x] 4.4 更新已有 `JobInfo` 记录时同步写入当前城市字段。

## 5. 发布求职城市写入

- [x] 5.1 在 `pages/pubilshJobSeek/pubilshJobSeek` 表单中展示当前省、市、区/县。
- [x] 5.2 在求职发布页展示小字提醒“只能发布本城市的信息”，且不提供表单内城市修改入口。
- [x] 5.3 提交 `JobSeeker` 时写入 `provinceName`、`cityName`、`districtName`、`provinceCode`、`cityCode`、`districtCode`、`cityDisplayName`。
- [x] 5.4 更新已有 `JobSeeker` 记录时同步写入当前城市字段。

## 6. 留言城市写入与展示

- [x] 6.1 在留言发布共享路径中读取当前城市。
- [x] 6.2 发布主留言时写入当前城市完整字段，并写入 `displayCityName` 作为留言城市展示字段。
- [x] 6.3 发布回复时写入当前城市完整字段，并写入 `displayCityName` 作为留言城市展示字段。
- [x] 6.4 确认留言列表和回复列表直接使用 `displayCityName` 展示城市名，不展示省名和区/县名。

## 7. Bmob 数据库更新提醒

- [x] 7.1 整理 `JobInfo` 需要新增的 Bmob 字段说明。
- [x] 7.2 整理 `JobSeeker` 需要新增的 Bmob 字段说明。
- [x] 7.3 整理 `MessageBoardMessage` 需要新增的 Bmob 字段说明，包含 `displayCityName`。
- [x] 7.4 在实现交付说明中告知历史 `JobInfo` 和 `JobSeeker` 需要在 Bmob 控制台手动补默认 `广东省 / 广州市 / 天河区` 城市字段，否则城市过滤后不可见。
- [x] 7.5 确认代码中不新增迁移脚本、云函数迁移或运行时自动补历史数据逻辑。

## 8. 验证

- [x] 8.1 验证首次进入默认展示 `广州`，本地缓存存在时展示缓存城市。
- [x] 8.2 验证开放城市可切换并缓存，未开放城市提示后不切换、不缓存。
- [x] 8.3 验证首页、今日招聘、今日求职只展示当前城市数据，切换城市后刷新。
- [x] 8.4 验证搜索结果只展示 `cityCode == 当前城市 cityCode` 且 (`title` 包含搜索关键词 OR `jobDescription` 包含搜索关键词 OR `companyName` 包含搜索关键词) 的岗位。
- [x] 8.5 验证发布 `JobInfo`、发布 `JobSeeker` 和发送留言均写入完整省市区和编码，且留言写入 `displayCityName`。
- [x] 8.6 验证发布页城市只读展示和小字提醒符合要求。
- [x] 8.7 验证留言列表直接使用 `displayCityName` 只展示城市名。
- [x] 8.8 验证首页、今日招聘、今日求职空结果展示“暂无信息”和“重新刷新”按钮。
- [x] 8.9 验证搜索结果可混合展示当前城市 `JobInfo` 和 `JobSeeker`，并使用不同 item 样式。
- [x] 8.10 验证首页左上角城市选择入口宽度已增大，且城市名省略规则不变。
