## 1. 数据模型与配置

- [ ] 1.1 定义留言板留言的 Bmob 集合/字段，包括 `targetType`、`targetId`、父留言 id、发布者元数据、内容、图片、位置、隐藏、加精、置顶和创建时间。
- [ ] 1.2 定义全局留言板开关和详情级留言开关的数据库配置记录。
- [ ] 1.3 定义全局敏感词记录，包含启用/停用状态，并支持通过数据库进行新增、删除、启用、停用和批量导入。
- [ ] 1.4 定义用户级全站留言禁言数据，以及发布留言或回复前使用的查询路径。

## 2. 共享留言板模块

- [ ] 2.1 创建可复用留言板组件/模块，接收宿主详情页传入的 `targetType`、`targetId` 和详情级开关状态。
- [ ] 2.2 实现 `globalMessageBoardEnabled && detailMessageEnabled` 的最终开关处理。
- [ ] 2.3 实现主留言分页加载，默认每页 10 条，并过滤隐藏留言。
- [ ] 2.4 实现主留言排序：置顶留言在前，非置顶留言在后，各组内按发布时间倒序。
- [ ] 2.5 实现留言行布局：左侧展示头像，右侧展示发布者名字、IP/城市位置、时间和留言正文。

## 3. 发布、回复与内容

- [ ] 3.1 实现发布权限校验，包括登录状态、全站禁言状态和留言板开关状态。
- [ ] 3.2 实现游客发布处理：展示 toast，并跳转/引导到个人中心登录流程。
- [ ] 3.3 实现主留言发布，支持文字、表情/emoji 内容和可选图片。
- [ ] 3.4 实现主留言下的一级回复发布，不创建嵌套回复层级。
- [ ] 3.5 实现回复展示：默认折叠，主留言发布者和当前用户自己的相关留言展开，回复按发布时间倒序展示且回复不支持置顶。

## 4. 图片与表情支持

- [ ] 4.1 使用微信选择图片组件/能力实现每条留言最多选择一张图片。
- [ ] 4.2 判断微信选择图片能力是否已完成压缩；如已压缩则不额外压缩，如未满足限制则执行补充压缩或拒绝。
- [ ] 4.3 实现图片最终校验，限制最终图片最大分辨率 1080x960、大小不超过 1MB。
- [ ] 4.4 实现留言图片预览。
- [ ] 4.5 在成本可控时实现微信风格表情 UI，并以 emoji 字符支持作为兜底。

## 5. 审核状态与敏感词

- [ ] 5.1 实现全局敏感词查询，只使用启用状态的敏感词记录。
- [ ] 5.2 实现主留言和回复发布时的敏感词过滤。
- [ ] 5.3 命中敏感词时仍创建留言记录，但保存为隐藏状态，并向发布者展示 toast 提示。
- [ ] 5.4 实现加精标识的渲染支持。
- [ ] 5.5 实现隐藏留言排除逻辑，确保隐藏留言对所有小程序用户不可见。
- [ ] 5.6 实现删除权限和按钮可见性：用户可删除自己的留言，普通用户在他人留言中看不到删除按钮，管理员可删除任意留言。

## 6. 详情页接入

- [ ] 6.1 在 `pages/jobDetail/jobDetail` 的最后一个普通内容区挂载留言板，使用 `targetType=job` 和当前 `jobId`，且不改变悬浮操作区域 `det-nav`。
- [ ] 6.2 在 `pages/seekerDetail/seekerDetail` 的最后一个普通内容区挂载留言板，使用 `targetType=job_seek` 和当前 `jobSeekId`，且不改变悬浮操作区域 `det-nav`。
- [ ] 6.3 确保挂载后的留言板与两个详情页现有 `det-nav` 悬浮区域保持安全间距，不影响 `det-nav` 的显示、点击和悬浮效果。
- [ ] 6.4 确保详情页不展示留言数量。

## 7. 绑定记录清理

- [ ] 7.1 识别所有职位详情删除路径，并在删除前弹窗提醒“删除该记录会同时删除他的留言信息，确认删除？”。
- [ ] 7.2 识别所有求职详情删除路径，并在删除前弹窗提醒“删除该记录会同时删除他的留言信息，确认删除？”。
- [ ] 7.3 用户确认删除后，物理删除 `targetType=job` 且 `targetId` 匹配被删除职位 id 的留言。
- [ ] 7.4 用户确认删除后，物理删除 `targetType=job_seek` 且 `targetId` 匹配被删除求职信息 id 的留言。
- [ ] 7.5 验证用户取消删除时，不删除详情记录，也不删除任何绑定留言。
- [ ] 7.6 验证绑定职位或求职详情被删除时，其回复也会被删除。

## 8. 验证

- [ ] 8.1 验证职位详情留言查询、发布、回复发布、排序、分页、删除前确认和删除行为。
- [ ] 8.2 验证求职详情留言查询、发布、回复发布、排序、分页、删除前确认和删除行为。
- [ ] 8.3 验证留言板作为详情页最后一个内容区展示，且不影响 `det-nav` 的显示、点击和悬浮效果。
- [ ] 8.4 验证全局开关、详情级开关、游客登录提示、用户禁言、隐藏、加精、置顶和敏感词命中流程。
- [ ] 8.5 验证微信选择图片、避免重复压缩、大小/分辨率拒绝、单图限制、图片预览和表情/emoji 展示。
