## 1. 数据快照

- [x] 1.1 使用项目内 `assets/pca-code.json` 作为全国省、市、区/县三级行政区划编码数据文件。
- [x] 1.2 确认 `assets/pca-code.json` 的数据来源为国家统计局《统计用区划代码和城乡划分代码》，并明确实际采用的快照年份。
- [x] 1.3 在查询工具或相邻元信息中记录本地文件路径、来源名称、来源入口和实际采用的快照年份，方便后续维护者追踪并替换数据快照。

## 2. 查询工具

- [x] 2.1 在 `utils/` 下新增独立的 CommonJS 地区编码工具模块，并读取或引用 `assets/pca-code.json`。
- [x] 2.2 实现 `findByDistrictCode(districtCode)`：处理首尾空白、校验 6 位数字格式，并在格式错误或区码不存在时返回 `null`。
- [x] 2.3 构建并复用懒加载区码索引，避免重复查询时反复遍历全国数据。
- [x] 2.4 查询成功时返回包含 `provinceCode`、`provinceName`、`cityCode`、`cityName`、`districtCode`、`districtName` 的扁平对象，并将源文件中的省级 2 位编码、市级 4 位编码补齐为 6 位编码。
- [x] 2.5 确保调用方修改返回对象不会污染缓存数据或影响后续查询结果。

## 3. 现有城市行为

- [x] 3.1 保持 `utils/city.js` 中当前城市缓存、展示和开放城市校验行为不变；除非明确需要，否则不接入新查询工具。
- [x] 3.2 确认新增全国地区编码查询能力不会让用户通过现有城市选择器选择或保存未开放业务城市。

## 4. 验证

- [x] 4.1 验证 `findByDistrictCode('440106')` 返回 `440000 / 广东省 / 440100 / 广州市 / 440106 / 天河区`。
- [x] 4.2 验证 `' 440106 '` 这类带首尾空白的入参可以解析为相同的天河区结果。
- [x] 4.3 验证格式错误编码和未知编码返回 `null`，且不会回退到默认城市。
- [x] 4.4 验证本地数据中存在但不属于开放业务城市的区码可以被查询，同时现有城市选择器仍会拒绝未开放业务城市。
- [x] 4.5 验证修改一次查询返回对象后，不会影响后续查询结果。
- [x] 4.6 检查新增本地数据大小，确认符合微信小程序包体积要求。
