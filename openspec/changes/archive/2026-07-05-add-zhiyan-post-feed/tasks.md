## 1. 数据模型与路由

- [x] 1.1 在实现交付说明或必要代码注释中记录 Bmob schema 要求：`Post`（`title`、`content`、`commitUid`、`photoImgs`、可选 `active`）和 `PostLike`（`postId`、`userId`）。
- [x] 1.2 在 `app.json` 中注册新路由 `pages/zhiyan/zhiyan` 和 `pages/zhiyanDetail/zhiyanDetail`。
- [x] 1.3 在 `app.json` 原生 tabBar 中新增名为“职言”的入口，入口位于“今日招聘”右侧；新增后展示顺序为：首页、今日招聘、职言、个人中心；`pagePath` 指向 `pages/zhiyan/zhiyan`，并配置有效的选中/未选中图标。
- [x] 1.4 按现有页面约定创建 `zhiyan` 列表页文件（`.js`、`.wxml`、`.wxss`、`.json`）。
- [x] 1.5 创建 `zhiyanDetail` 详情页文件（`.js`、`.wxml`、`.wxss`、`.json`），并在需要时注册现有 `message-board` 组件。

## 2. 帖子数据与互动辅助逻辑

- [x] 2.1 实现帖子规范化辅助逻辑，包括兜底文本、`photoImgs` 解析、展示 URL 转换、作者展示字段、列表/详情图片数量限制。
- [x] 2.2 实现 `Post` 分页加载，按更新时间或创建时间倒序排列；如果存在有效状态字段，则支持过滤有效帖子。
- [x] 2.3 通过帖子记录的 `commitUid` 补全 `_User` 作者信息，并为缺失作者记录或缺失字段提供安全兜底。
- [x] 2.4 实现基于 `Post.title` 和 `Post.content` 的关键词搜索，并处理搜索无结果状态。
- [x] 2.5 实现 `PostLike` 辅助逻辑：读取当前用户点赞状态、统计点赞数、创建点赞、取消点赞。

## 3. 职言列表页

- [x] 3.1 构建顶部搜索行，包含搜索输入框和右侧圆形加号发布图标。
- [x] 3.2 构建页面内 tabbar，只展示选中的“职言”类目。
- [x] 3.3 按 `local_source/zhiyan-list.jpg` 的风格构建帖子卡片，包含作者信息、标题、内容摘要、图片网格，以及底部分享/评论/点赞操作。
- [x] 3.4 当帖子图片超过 3 张时，列表页仅展示前 3 张。
- [x] 3.5 将列表页标题限制为最多 3 行，并仅在标题溢出时展示省略号和“全文”入口。
- [x] 3.6 当用户点击评论、“全文”或卡片详情入口时，跳转到 `zhiyanDetail`。
- [x] 3.7 对每张卡片使用微信原生分享，并确保分享标题、路径和图片对应当前选中的帖子。
- [x] 3.8 在列表页实现点赞切换：已点赞使用 `#fc3`，未点赞使用浅灰色；未登录用户点击时提示登录且不改变状态。
- [x] 3.9 实现列表分页、与页面结构一致的刷新行为、加载状态和空状态。
- [x] 3.10 列表页评论按钮在无评论时展示“评论”，有评论时展示“评论：<评论数>”。

## 4. 职言详情页

- [x] 4.1 根据路由参数 `postId` 加载选中的 `Post` 记录，并补全 `_User` 作者信息。
- [x] 4.2 按 `local_source/zhiyan-detail.jpg` 的风格构建详情 UI，包含作者信息、帖子标题、完整 `content` 和互动栏。
- [x] 4.3 在帖子内容末尾最多展示前 6 张图片，并支持图片预览。
- [x] 4.4 在详情页使用当前帖子标题、路径和图片实现微信原生分享。
- [x] 4.5 在详情页实现点赞切换，使用与列表页一致的 `PostLike` 持久化逻辑和颜色状态。
- [x] 4.6 在互动栏下方渲染帖子评论，数据来自 `MessageBoardMessage`，查询条件为 `targetType=post` 且 `targetId=<当前帖子 objectId>`。
- [x] 4.7 详情页评论按钮在无评论时展示“评论”，有评论时展示“评论：<评论数>”。

## 5. 验证

- [x] 5.1 验证帖子或作者字段缺失时，列表页和详情页展示可用兜底文本，而不是空白、`undefined` 或 `null`。
- [x] 5.2 验证列表卡片最多展示 3 张图片，详情页最多展示 6 张图片。
- [x] 5.3 验证长标题最多展示 3 行，且只有发生溢出的标题才展示“全文”入口。
- [x] 5.4 验证列表卡片和详情页点击分享时都会打开微信分享弹窗。
- [x] 5.5 验证评论操作会跳转详情页，详情页评论查询使用 `MessageBoardMessage`、`targetType=post` 和当前帖子 `objectId`。
- [x] 5.6 验证已登录用户可以在列表页和详情页点赞/取消点赞，未登录用户会收到登录提示且状态不变。
- [x] 5.7 验证发布图标在发布页不可用时不会产生路由错误或白屏。
- [x] 5.8 验证底部 tabBar 按 首页、今日招聘、职言、个人中心 的顺序显示，并且“职言”能打开包含帖子列表的 `pages/zhiyan/zhiyan`。
- [x] 5.9 验证列表页和详情页的评论按钮在无评论时展示“评论”，有评论时展示“评论：<评论数>”。
